module.exports = {
  languageOptions: {
    globals: {
      // Define global variables if necessary
    },
    parserOptions: {
      ecmaVersion: 2020,
    },
  },
  extends: [
    'eslint:recommended',
  ],
  rules: {
    // Add any specific rules here if needed
  },
};

